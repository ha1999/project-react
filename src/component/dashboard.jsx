import React from 'react';
import { Alert } from 'antd';

function Dashboard(props) {
    return (
        <div>
            <Alert message="dashboard" type="success" />
        </div>
    );
}

export default Dashboard;